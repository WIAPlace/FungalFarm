using UnityEngine;

// this is the ability to harvest a mushroom. will be added at runtime to the prefab by the host.
public class HarvenstInteractable : MonoBehaviour
{
    public Host host; // will be instantiated when this is added to an object
}
